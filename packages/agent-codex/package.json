{
	"name": "@eigent/agent-codex",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"build": "rm -rf dist && tsc -p tsconfig.build.json",
		"check-types": "tsc --noEmit",
		"clean": "rm -rf dist"
	},
	"dependencies": {
		"@eigent/agent-core": "workspace:*"
	},
	"devDependencies": {
		"@types/bun": "^1.2.14",
		"typescript": "^5.8.3"
	}
}
